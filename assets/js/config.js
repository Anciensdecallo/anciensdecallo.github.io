/* =====================================================================
   LES ANCIENS DE CALLO — RÉGLAGES DU SITE (le seul fichier à modifier)
   ===================================================================== */
window.CALLO = {
  // URL de l'application web Google Apps Script (se termine par /exec).
  // Tant qu'elle n'est pas renseignée, les formulaires tournent en mode démonstration.
  ENDPOINT: "",

  // Adresse de contact affichée pour les questions et les droits RGPD.
  CONTACT_EMAIL: "contact@anciens-callo.fr",

  // Date et heure de l'AG (compte à rebours).
  AG_DATE: "2026-12-18T18:30:00+01:00"
};
